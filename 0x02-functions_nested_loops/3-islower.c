#include "holberton.h"
/**
 * _islower - Verifies if letter are lowercase
 *
 *
 * Return: 1 if lowercase detecde else return  Nothing
 */
int _islower(int c)
{
while (c > 96 && c < 123)
{
return (1);
}
return (0);
}
